<Page x:Class="LabAssistant.Views.TemplateEditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="TemplateEditorPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Template Editor" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                BorderBrush="#FFDDDDDD"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,12">
                    <TextBlock Text="Template Id" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Height="28"
                             Text="{Binding Template.Id, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Version" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Height="28"
                             Text="{Binding Template.Version, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,12,0">
                    <TextBlock Text="Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Height="28"
                             Text="{Binding Template.Name, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="1">
                    <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox Height="28"
                             Text="{Binding Template.Description, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                BorderBrush="#FFDDDDDD"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <StackPanel>
                <DockPanel Margin="0,0,0,10">
                    <TextBlock Text="Virtual Machines"
                               FontSize="18"
                               FontWeight="SemiBold"
                               DockPanel.Dock="Left"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                        <Button Content="Add VM" Width="90" Margin="0,0,8,0" Click="AddVm_Click"/>
                        <Button Content="Remove VM" Width="110" Click="RemoveVm_Click"/>
                    </StackPanel>
                </DockPanel>

                <DataGrid x:Name="VmDataGrid"
                          ItemsSource="{Binding VmTemplates}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          Height="260">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name"
                                            Width="160"
                                            Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Memory (MB)"
                                            Width="110"
                                            Binding="{Binding MemoryMb, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="CPU"
                                            Width="70"
                                            Binding="{Binding CpuCount, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="Switch Name"
                                            Width="140"
                                            Binding="{Binding SwitchName, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="VHDX Id"
                                            Width="140"
                                            Binding="{Binding VhdxId, UpdateSourceTrigger=PropertyChanged}"/>
                        <DataGridTextColumn Header="VHDX Path"
                                            Width="220"
                                            Binding="{Binding VhdPath, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
