<Page x:Class="LabAssistant.Views.TemplateEditorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="TemplateEditorPage">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Template Editor" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                BorderBrush="#FFDDDDDD"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,12,12">
                    <TextBlock Text="Template Id" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox x:Name="TemplateIdBox" Height="28"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Version" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox x:Name="TemplateVersionBox" Height="28"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,12,0">
                    <TextBlock Text="Name" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox x:Name="TemplateNameBox" Height="28"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="1">
                    <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBox x:Name="TemplateDescriptionBox" Height="28"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                BorderBrush="#FFDDDDDD"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Virtual Machines" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <ListView x:Name="VmListView" Height="260">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="180"/>
                            <GridViewColumn Header="CPU" Width="80"/>
                            <GridViewColumn Header="Memory (MB)" Width="120"/>
                            <GridViewColumn Header="VHDX" Width="240"/>
                        </GridView>
                    </ListView.View>
                    <ListViewItem>
                        <Border BorderBrush="#FFCCCCCC"
                                BorderThickness="1"
                                CornerRadius="4"
                                Background="#FFF8F8F8"
                                Padding="12"
                                Margin="6">
                            <TextBlock Text="No VMs yet. This area will list template VMs."
                                       FontStyle="Italic"
                                       Foreground="#FF666666"/>
                        </Border>
                    </ListViewItem>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</Page>
