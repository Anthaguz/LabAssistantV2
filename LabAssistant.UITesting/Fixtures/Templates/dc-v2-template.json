{
  "schemaVersion": "2.0.0",
  "id": "REPLACED_AT_SEED_TIME",
  "name": "REPLACED_AT_SEED_TIME",
  "description": "Harness-seeded single first-domain-controller V2 template. One forest (smoke.lab / SMOKE) with a single RootDomainController VM on a static 10.0.0.10/24 NIC that serves its own DNS. The VM references the REAL prepared Windows Server base image (guest-configurable via its bootstrap profile) so the V2 plan requires guest work - DC promotion - and can only start once the local bootstrap credential slot is filled. Only localBootstrap is authored; the planner reuses it for domain-admin and DSRM. Safe to delete.",
  "templateRevision": 1,
  "createdWithAppVersion": "0.0.0",
  "templateType": "lab-template",
  "deploymentProfile": "Balanced",
  "directoryTopology": {
    "forests": [
      {
        "forestId": "forest-smoke",
        "rootDomainId": "domain-smoke"
      }
    ],
    "domains": [
      {
        "domainId": "domain-smoke",
        "dnsName": "smoke.lab",
        "netBiosName": "SMOKE",
        "forestId": "forest-smoke",
        "relationKind": "Root",
        "firstDomainControllerVmId": "REPLACED_AT_SEED_TIME"
      }
    ],
    "trusts": []
  },
  "labNetworks": [
    {
      "networkId": "smoke-net",
      "name": "Smoke",
      "switchName": "REPLACED_AT_SEED_TIME",
      "subnet": "10.0.0.0/24"
    }
  ],
  "vmTemplates": [
    {
      "vmId": "REPLACED_AT_SEED_TIME",
      "name": "REPLACED_AT_SEED_TIME",
      "memoryMb": 2048,
      "cpuCount": 2,
      "vhdxId": "REPLACED_AT_SEED_TIME",
      "topologyRole": "FirstDomainController",
      "domainId": "domain-smoke",
      "capabilityRoles": [],
      "credentialSlots": {
        "localBootstrap": "disk.winserver2022.local-admin"
      },
      "nics": [
        {
          "nicId": "primary",
          "networkId": "smoke-net",
          "ipAddress": "10.0.0.10",
          "prefixLength": 24,
          "dnsServers": [
            "10.0.0.10"
          ]
        }
      ]
    }
  ]
}
