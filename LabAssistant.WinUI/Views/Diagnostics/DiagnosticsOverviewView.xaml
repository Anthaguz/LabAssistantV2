<UserControl
    x:Class="LabAssistant.WinUI.Views.Diagnostics.DiagnosticsOverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid
        RowSpacing="12"
        AutomationProperties.LandmarkType="Main"
        XYFocusKeyboardNavigation="Enabled">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Foreground="{StaticResource ShellTextSecondaryBrush}"
            Text="Use Diagnostics to inspect structured log activity and support signals without leaving the capability workspace."
            TextWrapping="Wrap" />

        <Grid
            Grid.Row="1"
            ColumnSpacing="12"
            AutomationProperties.LandmarkType="Navigation"
            XYFocusKeyboardNavigation="Enabled">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Padding="{StaticResource CardPadding}"
                Background="{StaticResource ShellBackgroundBrush}"
                BorderBrush="{StaticResource ShellBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}">
                <StackPanel Spacing="{StaticResource SpacingS}">
                    <TextBlock
                        Foreground="{StaticResource ShellTextPrimaryBrush}"
                        FontWeight="SemiBold"
                        Text="Logs" />
                    <TextBlock
                        Foreground="{StaticResource ShellTextSecondaryBrush}"
                        Text="{x:Bind ViewModel.LogsSummaryText, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <Button
                        HorizontalAlignment="Left"
                        TabIndex="0"
                        AccessKey="L"
                        AutomationProperties.Name="Open diagnostics logs"
                        AutomationProperties.HelpText="Open the structured logs workspace."
                        Command="{x:Bind ViewModel.OpenLogsCommand}"
                        Content="Open Logs" />
                </StackPanel>
            </Border>

            <Border
                Grid.Column="1"
                Padding="{StaticResource CardPadding}"
                Background="{StaticResource ShellBackgroundBrush}"
                BorderBrush="{StaticResource ShellBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}">
                <StackPanel Spacing="{StaticResource SpacingS}">
                    <TextBlock
                        Foreground="{StaticResource ShellTextPrimaryBrush}"
                        FontWeight="SemiBold"
                        Text="Support Export" />
                    <TextBlock
                        Foreground="{StaticResource ShellTextSecondaryBrush}"
                        Text="{x:Bind ViewModel.SupportSummaryText, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <Button
                        HorizontalAlignment="Left"
                        TabIndex="1"
                        AccessKey="O"
                        AutomationProperties.Name="Open log location"
                        AutomationProperties.HelpText="Open the current log file location for support export."
                        Command="{x:Bind ViewModel.OpenSupportExportCommand}"
                        Content="Open Log Location" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
